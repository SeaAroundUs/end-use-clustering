# install with conda env create -f environment.yml
name: 'end-use'
channels:
  - conda-forge
  - plotly
  - defaults
dependencies:
  - python==3.12
  - ipykernel
  - ipywidgets
  - pandas
  - matplotlib
  - plotly
  - jupyter-dash
  - scikit-learn